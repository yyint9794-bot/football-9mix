name = "football-9mix"
compatibility_date = "2024-09-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

# KV: bind WALLET_KV in Pages Dashboard → Settings → Functions (not here until you have real IDs).
